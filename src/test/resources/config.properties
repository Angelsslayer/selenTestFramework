#WebDriver configs
IMPLICIT_WAIT_SECONDS=5
EXPLICIT_WAIT_SECONDS=3
BASE_URL=http://localhost:4205/#

#log4j configs, for the file "log4j.properties"
log.path=test.log                  # Path to logs -> to avoid errors, make sure that the folders specified in the path exist!
log.timezone=UTC                   # Time zone for logs
log.packages=com.softserve.edu13   # Main package where should the logs be collected
log.level=INFO,ERROR,WARN          # Logging levels